services:
  acies-geo:
    image: acies-pi:latest
    profiles: [geo, geo-classifier, multimodal]
    command: acies-geo
    network_mode: host
    environment:
      - PATH=/acies-os/.venv/bin:/usr/local/bin:/usr/bin:/bin
      - ACIES_LOG=${ACIES_LOG:-}
      - ACIES_HOST=${ACIES_HOST:-}
      - ACIES_NAME=geo
      - ACIES_CONNECT=${ZENOH_CONNECT:-tcp/127.0.0.1:7501}
    volumes:
      - /data:/data
      - ~/.acies:/root/.acies
    devices:
      - /dev/serial0:/dev/serial0
      - /dev/bus/usb:/dev/bus/usb
    group_add:
      - dialout
    restart: unless-stopped

  acies-mic:
    image: acies-pi:latest
    profiles: [mic, mic-classifier, multimodal]
    command: acies-mic
    network_mode: host
    environment:
      - PATH=/acies-os/.venv/bin:/usr/local/bin:/usr/bin:/bin
      - ACIES_LOG=${ACIES_LOG:-}
      - ACIES_HOST=${ACIES_HOST:-}
      - ACIES_NAME=mic
      - ACIES_CONNECT=${ZENOH_CONNECT:-tcp/127.0.0.1:7501}
    volumes:
      - /data:/data
      - ~/.acies:/root/.acies
    devices:
      - /dev/snd:/dev/snd
    group_add:
      - audio
    restart: unless-stopped

  acies-vfm:
    image: acies-pi:latest
    profiles: [geo-classifier, mic-classifier, multimodal]
    command: acies-vfm --weight ${VFM_WEIGHT} --geo ${ACIES_HOST:-}/geo --mic ${ACIES_HOST:-}/mic
    network_mode: host
    environment:
      - PATH=/acies-os/.venv/bin:/usr/local/bin:/usr/bin:/bin
      - ACIES_LOG=${ACIES_LOG:-}
      - ACIES_HOST=${ACIES_HOST:-}
      - ACIES_NAME=vfm
      - ACIES_CONNECT=${ZENOH_CONNECT:-tcp/127.0.0.1:7501}
    volumes:
      - /data:/data
      - /model:/model
      - ~/.acies:/root/.acies
    restart: unless-stopped

  zenohd:
    image: acies-pi:latest
    profiles: [geo, mic, geo-classifier, mic-classifier, multimodal]
    command: zenohd --listen ${ZENOH_CONNECT:-tcp/127.0.0.1:7501} ${ZENOH_BRIDGE}
    network_mode: host
    volumes:
      - ~/.acies:/root/.acies
    restart: unless-stopped
