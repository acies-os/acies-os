[project]
name = "acies-sensors"
version = "0.1.2"
description = "Sensor services for AciesOS (microphone, geophone)."
requires-python = ">=3.10"
dependencies = [
    "acies-core",
    "msgspec>=0.20",
    "numpy>=1.24",
    "rawshake",
    "sounddevice>=0.5.5",
]

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[tool.hatch.metadata]
allow-direct-references = true

[tool.hatch.build.targets.wheel]
packages = ["src/acies"]

[project.scripts]
geo = "acies.sensors.geo:main"
mic = "acies.sensors.mic:main"

[tool.uv.sources]
acies-core = { workspace = true }
rawshake = { git = "https://github.com/jinyangustc/rawshake.git", tag = "v0.1.3" }
